;; Link descriptions for zk links should always be the same as the
;; files title.
(defun org-zk-lint-link-descriptions ()
  ;; TODO
  )

(defun org-zk-link-dead-links ()
  ;; TODO
  )

(provide 'org-zk-lint)
